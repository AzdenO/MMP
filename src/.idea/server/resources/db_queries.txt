CREATE TABLE Users (userid VARCHAR(50) NOT NULL UNIQUE, memberid VARCHAR(8),platformid VARCHAR(16), platformtype VARCHAR(1), characters JSON, refresh_token VARCHAR(256), token_expiry VARCHAR(30), PRIMARY KEY (userid))/--/CREATE TABLE Weapons (instanceid VARCHAR(16) NOT NULL UNIQUE, itemid VARCHAR(16),userid VARCHAR(50) NOT NULL UNIQUE,name VARCHAR(50),power VARCHAR(4),type VARCHAR(15), damage_type VARCHAR(20), stats JSON, perks JSON,PRIMARY KEY (instanceid), FOREIGN KEY (userid) REFERENCES Users(userid))/--/CREATE TABLE Armors (instanceid VARCHAR(16) NOT NULL UNIQUE, itemid VARCHAR(16),userid VARCHAR(50) NOT NULL UNIQUE,name VARCHAR(50),power VARCHAR(4),type VARCHAR(15), stats JSON, perks JSON, PRIMARY KEY (instanceid), FOREIGN KEY (userid) REFERENCES Users(userid))/--/